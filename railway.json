{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "docker compose build && docker compose up -d && echo 'Please wait... Waiting for MySQL connection...'"
    }
}